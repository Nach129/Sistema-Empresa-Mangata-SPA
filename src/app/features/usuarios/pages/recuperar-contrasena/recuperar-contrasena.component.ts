import{Component,inject,signal}from'@angular/core';import{FormControl,FormGroup,ReactiveFormsModule,Validators}from'@angular/forms';import{RouterLink}from'@angular/router';import{AutenticacionService}from'../../../../core/services/autenticacion.service';
@Component({selector:'app-recuperar-contrasena',imports:[ReactiveFormsModule,RouterLink],templateUrl:'./recuperar-contrasena.component.html',styleUrl:'../recuperacion-contrasena.css'})export class RecuperarContrasenaComponent{private readonly autenticacion=inject(AutenticacionService);readonly enviando=signal(false);readonly enviado=signal(false);readonly error=signal('');readonly formulario=new FormGroup({correo:new FormControl('',{nonNullable:true,validators:[Validators.required,Validators.email]})});async enviar(){if(this.formulario.invalid||this.enviando())return;this.enviando.set(true);this.error.set('');try{await this.autenticacion.solicitarRecuperacion(this.formulario.getRawValue().correo.trim().toLowerCase());this.enviado.set(true);}catch{this.error.set('No fue posible enviar el correo. Intenta nuevamente en unos minutos.');}finally{this.enviando.set(false);}}}
